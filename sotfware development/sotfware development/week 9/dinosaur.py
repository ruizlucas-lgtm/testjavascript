class Player: 

    def __init__(self, name, species, diet, age):
        self.name = name
        self.species = species
        self.diet = diet  
        self.age = age

    def roar(self):
        print(f"{self.name} lets out a ROAR!")

    def eat():
    if:
        eat(self, meat)
        print(f"{self.name} eats raw meat happily! ")
    else: 
        eat(self, plant)
        print(f"{self.name}")
